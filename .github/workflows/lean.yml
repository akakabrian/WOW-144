name: Verify WOWII 144

on:
  push:
    branches:
      - main
      - 'proof/**'
      - 'agent-**'
    paths-ignore:
      - lean-ci.log
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install elan
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y --default-toolchain none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"

      - name: Resolve dependencies
        run: lake update

      - name: Restore Formal Conjectures build cache
        uses: actions/cache@v4
        with:
          path: .lake/packages/formal_conjectures/.lake/build
          key: wow144-formal-build-${{ hashFiles('lean-toolchain', 'lakefile.toml') }}

      - name: Download Mathlib cache
        run: lake exe cache get

      - name: Verify and capture compiler log
        id: verify
        shell: bash
        run: |
          set +e
          {
            echo '$ lake build FormalConjectures.WrittenOnTheWallII.GraphConjecture144'
            lake build FormalConjectures.WrittenOnTheWallII.GraphConjecture144
            echo '$ lake env lean -DwarningAsError=true WOW144/Audit.lean'
            lake env lean -DwarningAsError=true WOW144/Audit.lean
            echo '$ lake env lean -DwarningAsError=true WOW144/Arithmetic.lean'
            lake env lean -DwarningAsError=true WOW144/Arithmetic.lean
            echo '$ lake --wfail build WOW144'
            lake --wfail build WOW144
            echo '$ local sorry/axiom scan'
            if grep -RInE '(^|[^[:alnum:]_])(sorry|axiom)([^[:alnum:]_]|$)' WOW144.lean WOW144; then
              echo 'Local proof artifacts contain sorry or axiom.' >&2
              exit 1
            fi
          } > lean-ci.log 2>&1
          status=$?
          echo "exit_status=$status" >> "$GITHUB_OUTPUT"
          printf '\nEXIT_STATUS=%s\n' "$status" >> lean-ci.log
          exit 0

      - name: Upload compiler log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lean-ci-log-${{ github.run_id }}
          path: lean-ci.log

      - name: Publish compiler log on branch pushes
        if: github.event_name == 'push'
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -f lean-ci.log
          git commit -m "ci: publish Lean compiler log" || true
          git push origin HEAD:${GITHUB_REF_NAME}

      - name: Fail job when Lean failed
        if: steps.verify.outputs.exit_status != '0'
        run: exit 1
